# v0.5 — Test Matrix

**Status:** Draft
**Date:** 2026-07-15

Normative references: ADR-021, v0.5-protocol-contract.md, v0.4-test-matrix.md.

---

## 1. Area: Desktop Derivation

| # | Test | Behaviour |
|---|------|-----------|
| 1 | active Profile association preserved | CSS profile_id == active_profile_id |
| 2 | Profile display name preserved | CSS profile_name == Profile.name |
| 3 | Page order preserved | CSS pages[*] order matches Vec<Page> index |
| 4 | Page identity preserved | CSS pages[*].page_id == Page.id string |
| 5 | Page names preserved | CSS pages[*].name == Page.name |
| 6 | Button order preserved per Page | CSS pages[*].buttons[*] order matches Vec<Button> index |
| 7 | Button identity preserved | CSS pages[*].buttons[*].button_id == Button.id string |
| 8 | Button labels preserved | CSS pages[*].buttons[*].label == Button.label |
| 9 | ActionReference absent from CSS | DTO excludes ActionReference, action_name, payload |
| 10 | active Profile rename changes derivation | profile_name differs → publish |
| 11 | Page add changes derivation | pages array changes → publish |
| 12 | Page rename changes derivation | pages[*].name differs → publish |
| 13 | Page remove changes derivation | pages array shrinks → publish |
| 14 | Button add changes derivation | buttons[*] array grows → publish |
| 15 | Button update (relabel) changes derivation | buttons[*].label differs → publish |
| 16 | Button remove changes derivation | buttons[*] array shrinks → publish |
| 17 | Button reorder changes derivation | buttons[*] array order differs → publish |
| 18 | inactive Profile edit leaves derivation unchanged | CSS == previous → no publish |
| 19 | no active Profile (null) derives null triple | profile_id=null, profile_name=null, pages=null |
| 20 | active Profile with zero Pages derives pages=[] | profile_id=string, pages=[] |
| 21 | unresolved active Profile = derivation failure | No publish, channel unchanged |

## 2. Area: Publication

| # | Test | Behaviour |
|---|------|-----------|
| 22 | Initial retained CSS snapshot available | watch::Sender initialized with None, first publish sets value |
| 23 | Latest-value replacement | P1 published → P2 published → receiver sees P2 |
| 24 | Duplicate derivation suppression | Derivation equals previous → send_replace NOT called |
| 25 | Slow consumer skips intermediate states | P1 → P2 → P3, consumer processes P1 late → changed() yields true, borrow() returns P3 |
| 26 | Same active identity + surface edit publishes CSS | APS unchanged, CSS republished |
| 27 | inactive Profile edit does not logically change CSS | No CSS publish |
| 28 | active Profile transition derives associated CSS | New Profile → new CSS |

## 3. Area: Trust

| # | Test | Behaviour |
|---|------|-----------|
| 29 | identified untrusted client receives no CSS | No CSS Receiver cloned → no delivery |
| 30 | trusted client receives latest CSS | CSS Receiver cloned after trust → delivery |
| 31 | pair acceptance activates CSS | pair_accepted → CSS delivery |
| 32 | trusted reconnect receives latest CSS | New WebSocket → identify → trusted → borrow returns latest CSS |

## 4. Area: Android Parser (Strict)

| # | Test | Behaviour |
|---|------|-----------|
| 33 | valid nested CSS v1 accepted | Parses all levels: type, version, profile_id, profile_name, pages |
| 34 | schema_version != 1 rejected | Log, ignore |
| 35 | fractional schema_version rejected | Log, ignore |
| 36 | missing required field rejected | Log, ignore |
| 37 | wrong field type rejected | Log, ignore |
| 38 | nullability invariant violation rejected | profile_id=null+profile_name=non-null, etc. Log, ignore |
| 39 | malformed Page entry rejected | Log, ignore entire CSS |
| 40 | malformed Button entry rejected | Log, ignore entire CSS |
| 41 | Page order retained on valid CSS | pages array index preserved |
| 42 | Button order retained per Page | buttons array index preserved, per Page |
| 43 | no-active null triple accepted | profile_id=null, profile_name=null, pages=null |
| 44 | active Profile with pages=[] accepted | profile_id=string, profile_name=string, pages=[] |
| 45 | invalid update does not replace valid retained CSS | Invalid CSS received → retained CSS unchanged |
| 46 | valid CSS replaces retained CSS | Valid CSS received → retained CSS updated |
| 47 | APS and CSS retained independently | APS update does not clear CSS, CSS update does not clear APS |

## 5. Area: Association / Eligibility

| # | Test | Behaviour |
|---|------|-----------|
| 48 | matching APS+CSS = eligible | APS profile_id == CSS profile_id → eligible=true |
| 49 | mismatching APS+CSS retains CSS, ineligible | APS profile_id != CSS profile_id → eligible=false, CSS retained |
| 50 | APS null → no CSS eligible | APS profile_id=null → eligible=false for any CSS |
| 51 | APS gaming → CSS null → no eligible CSS | CSS null triple → eligible=false |
| 52 | APS(B) then CSS(B) convergence | APS updates → mismatch, CSS updates → eligible |
| 53 | CSS(B) then APS(B) convergence | CSS updates → retained, ineligible, APS updates → eligible |
| 54 | temporary mismatch is architecturally safe | System tolerates any arrival order |
| 55 | valid mismatching CSS replaces older retained CSS | CSS coding received while APS gaming → retained CSS = coding (was prev), ineligible |

## 6. Area: Android Spike (Diagnostic)

| # | Test | Behaviour |
|---|------|-----------|
| 56 | diagnostic hierarchy output | Log/display: Profile name + Page list + Button list |
| 57 | diagnostic eligibility output | Log/display: eligible=true/false with profile_id reason |
| 58 | no button interaction | No tap handling, no execution sender, no action_request |
| 59 | no execution request wiring | UI/components do not send action messages |

## 7. Area: Compatibility

| # | Test | Behaviour |
|---|------|-----------|
| 60 | ActiveProfileStateMessage v1 unchanged | Serialization identical to v0.4 |
| 61 | existing v0.4 Android APS tests valid | APS handling unchanged |
| 62 | existing inbound action handler unchanged | Action processing unchanged |
| 63 | action_result unchanged | Action result format unchanged |
| 64 | unknown additive message compatibility | Mobile ignores completely unknown message types |

## 8. Coverage Summary

| Area | Tests |
|------|-------|
| Desktop Derivation | 21 |
| Publication | 7 |
| Trust | 4 |
| Android Parser | 15 |
| Association / Eligibility | 8 |
| Android Spike (Diagnostic) | 4 |
| Compatibility | 5 |
| **Total (v0.5)** | **64** |

All v0.4 architectural and Android tests remain valid and unchanged.

No assigned, ActionReference, action_name, payload, or execution-derived
state appears in the test matrix.
